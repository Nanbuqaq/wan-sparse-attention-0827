# Controlled recent-window follow-up

The precision10-case153 screen is mixed: risk-wire improves motion Dense fidelity but not state fidelity versusFinal. It is not a promoted co-design winner.

The next bounded diagnostic changes **only the dense local window**, equally for baseline and precision: legacyFinal25, SDPA-null25, wire mass14 and wire risk14; two development categories, seed20260904,39latent/153pixels. Eight cases, with `exact_local_window_frames=8` (current3 + up to5 completed local frames, plus sink1). Existing same-hardware default-window screen is the control; first compare unchanged settings and noise identity before interpreting effects. Raw history bytes must not increase simply from enlarging the resident exact window, but attention work can increase.

This is a method/context allocation ablation, not a same-RoutePlan system optimization. Current first-pass query values and therefore routes may change. Do not claim unchanged route SHA or pixel equality against the old local window. All arms use the same direct-output RoPE and cached archive implementation.

No automatic promotion follows. Review absolute late state/identity behavior separately from Dense-relative fidelity. Any useful allocation must also be offered to Final, and later long-video tests must distinguish the generic recent-context benefit from precision admission.
