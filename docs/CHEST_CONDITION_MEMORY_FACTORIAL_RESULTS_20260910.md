# Condition and raw memory interact; late-state reliability is still incomplete

Two new hybrid509 cases complete the2x2 with six reused cells. Both pass actual
pre96/pre381 RGB equality against the SAME-current-text controls. The frozen
causal selector again chooses source40–47 without forced indices; each archives
7.786GB and retrieves2.595GB. Both retain reference construction, not the new
startup option, so no loading advantage is attributed to the hybrid.

| Current condition | No extra history | Causal recent history |
| --- | --- | --- |
| Original anaphoric return | closed state | closed state / exterior color fragments |
| Exact past state text appended | coarse open/blue state; source details change | source-specific appearance is visibly closer in both seeds |

The interaction is a descriptive development signal, not a robust quality win.
Hybrid seed25 still lowers its lid late (without the text-only hand in sampled
frames). Hybrid seed26 preserves the broad open state in sampled late frames,
with wood/front ornament/shiny cloth more source-like than text-only. No pixel-
exact source reconstruction, blind human rating or every-frame playback is claimed.

The registered both-seed unchanged-state gate is NOT satisfied. Keep this mixed
result: current state cues and archived appearance complement one another, but
future state evolution/commitment remains unresolved. Do not tune more phrases,
seeds or rebind deltas on these same cases or call this autonomous state memory.

Facts: `results/metrics/memory_activation_20260910/chest_condition_history509_v1/`
per-seed audits/boards and semantic_review.json. No SOTA/Pareto/time ranking.
