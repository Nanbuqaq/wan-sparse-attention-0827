# 历史K位置：首轮结论与固定复验

首轮 `key_position509_v1` 新增3条、复用原related-original控制，4格技术通过。
四格实际pre96 latent及前381张完整解码RGB逐位一致，H2D均2,595,225,600 bytes。
没有清额外上下文、复制源或改变Attention容量。相关源时间delta64，away源delta40。

首轮视频观察（seed20260913）：

| 来源 | 原位置 | recent virtual位置 |
| --- | --- | --- |
| 红珠相关源40–47 | 返回空罐，晚段又倒入透明液体 | 首返回红珠恢复，晚段仍新增倒液体动作 |
| away花朵源56–63 | 花朵侵入罐子/背景，晚段黄色颗粒 | 花朵和草地持续侵入，不恢复红珠 |

结论是固定工作负载内的位置干预证据，不是完整长期质量赢家。源40–47仍在落珠，
因此恢复信息与复用动作纠缠。错误来源不因重绑定变正确；其KV也可能带有历史上下文化，
不能声称它信息论上完全不包含红珠。recent额外算子时间约数十毫秒，不能据单次计时声称加速。

## 在看复验结果前冻结

- 新开发seed20260921，不使用原待执行H20的seed13/14；不是正式holdout。
- 同一已有代码fb1afb0、同prompt、同原生509、同BF16/FA2/Triton3.2、同容量32。
- 四格全部执行，不根据第一条结果挑选后续格；GPU0两related、GPU1两away，物理锁。
- 各格同seed/noise/pre96 latent/前381 decodedRGB/原生源码/预算；若不一致则不能配对归因。
- 不改变delta/源位置，不再调参。保持技术失败和negative，不追选seed。
- 判读分别记录：源内容有效性、首返回原状态类别、错误场景侵入、晚段新增动作。
- 若两seed不一致，记录位置作用依赖轨迹，不推广统一recent策略。
- 即使两seed类别恢复，仍需静止源与第二类主体检验，且不能推出自主admission成立。

只本地运行，不push、不新投InferHub；权限边界不变。
