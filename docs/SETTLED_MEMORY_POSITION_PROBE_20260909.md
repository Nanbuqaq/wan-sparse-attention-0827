# 较静止源的既定位置验证

依据已完成settled-source Dense筛选及无cut/cut组件控制，冻结新development探针。
原screen的文字、视频和判读不修改：末8帧红珠可辨识、相对稳定，但仍有残余运动/罐外珠，
不是严格物理守恒的真值；两个可见控制曾自行丢状态，不能称这是纯长期记忆任务。

- 配置`native_settled_memory_probe.json`记录已审查文档与技术审计SHA。
- seeds20260919/20，原生509，每seed original/recent两条正确源shot8，同预算、同容量32。
- 同seed旧Dense `settled_state_screen509_v1/laneN/settled_bead_revisit`只读复用。
- 只从实际away边界48向前取source40–47；五阶段的segments[2]是32，不能照搬旧固定下标。
- recent仍为已经固定的delta64，不按正在运行的phase/age组件结果挑策略。
- 首个seed19-original充当真实全流程runtime门禁；通过实际pre96 latent/前381 decodedRGB及source坐标/字节审计后，才允许后续三条。
- 不把该原生gate另算独立质量样本。无低分辨率替代、无Dense重跑、无teacher在线输入。
- 首返回类别、填充外观、场景污染和晚段多余动作分开判读；两seed都报告，不筛成功seed。
- 这个实验只能判断在较静止源下是否仍发生动作污染，不能单独证明“过去动作编码进KV”的因果效应，因prompt/源轨迹也改变了。

当前只完成配置与CPU门禁准备，未启动本批GPU，不得当成已有质量结果。
