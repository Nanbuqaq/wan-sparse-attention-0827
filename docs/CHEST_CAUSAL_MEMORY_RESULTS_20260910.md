# Independent chest: correct admission, no correct state restoration

The corrected frozen runtime822155c completed all4 new509-frame memory videos;
6/6 technical cases including the reused Dense controls pass, missing0. Both
seed25/26 use exactly the same pre96 latent/pre381 decoded RGB across methods.
Original/recent share archive decisions and byte counts; the unchanged causal
selector independently chooses the open source40–47 at return96 on both seeds.
No source frames or Dense output enter selection.

Neither position restores the open lid or the cloth-inside state on either seed.
Seed25 memory removes the large foreground daisy but leaves the chest closed.
Seed26 original leaves a pale petal-like artifact; recent leaves blue fragments
on the exterior of a closed chest. These are not correct state restoration.
The descriptive inspection covers selected-source, first/late-return and paired
boards, not blind scoring or continuous every-frame playback.

All4 memory runs archive7.786GB and retrieve2.595GB; peak CPU archive7.786GB is
within8GiB. Archive wall observations~2.29–2.38s, install~0.56–0.61s, selector
~0.00037–0.00041s; recent adds~0.022s recorded rebind wall within installation.
These are individual run observations, not speedup tests; do not double-count
rebind inside install or call this a25% physical-history-budget comparison.

The earlier red-bead signal cannot support a general state-memory method.
Selecting a correct episode, increasing its influence, and reconstructing the
right spatial/physical relations are separate requirements. Color/appearance
bleed is a failure mode, not partial semantic completion scored as success.
Visible-cut controls also failed, so this is not isolated long-absence forgetting.
No prompt, seed, similarity threshold or rebind delta is retuned on these results.

Facts: `results/metrics/memory_activation_20260910/chest_causal_memory509_review_v2/`
contains technical_audit.json, semantic_review.json, interval/native boards and
index.html. V1's four preflight failures remain separately preserved; they never
generated method videos and are not four extra scientific negative samples.
