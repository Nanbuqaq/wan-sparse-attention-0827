# Two-case realizability control after the independent chest negative

This is a new diagnostic, not a retuned memory candidate or a new holdout result.
Keep the completed four-case negative untouched. On the same two source-qualified
seeds25/26, append the exact previously present settled-source prompt to each
return prompt. Original return cue, all earlier prompts, cuts2/6/12, native
configuration, resolution and seed remain unchanged. No history KV bank/router.

One source-text operation only; no alternative wording or extra seed search.
CPU checks exercise both exact CLI cases and verify the unchanged pre-return
schedule. First full case must pass actual pre96/pre381 RGB and native branch
audits before the other case. All outcomes stay separate from the old memory
comparison and original Dense source screen.

Question: can explicit access to the previously described state generate an
open chest with contents inside, when raw-memory position variants could not?
A positive control would show that state geometry is realizable under a changed
current condition, not that a KV router recovered it. A negative would leave
more fundamental native transition/context constraints unresolved. Either way,
past requested text is not a general completion detector, and this structured
source choice is not an autonomous online state tracker. No novelty/SOTA claim.
