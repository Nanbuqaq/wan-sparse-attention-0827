# Independent object-state feasibility screen

Do not continue prompt tuning on the failed blue-canvas workload. Freeze two
new state categories and two unused development seeds20260925/26: an opened
walnut chest with blue cloth, and a torn envelope with a partly inserted red card.
The chest is configuration state, not an irreversible-state claim. No OCR,
exact tear geometry, or object-count score is required.

Four scenarios x two seeds = eight Dense-only native509 videos. Each pair shares
the initial/change/settled prompts up to latent48. Revisit shows an unrelated
flower from48 to96; the visible control retains the object while keeping the
same native cut indices2/6/12. Return text does not restate the state details.

The source must visibly achieve the registered state at latent40–47 in both
seeds before any new memory method is run. The late-away target must actually
be absent. Check actual first48 latents/first189 decoded pixels agree within
each revisit/control pair; source quality, visible-control drift and revisit
memory failure are separate findings. Do not count a source-generation failure
as a retrieval failure. No post-result prompt/seed search in this screen.

Use unmodified native5B BF16/FA2,704x1280,CFG1 positive-only cache,local32,
fixed adaLN16/stage1, native VAE. No episode/causal memory, teacher capture,
pipeline or RoPE intervention. This is development feasibility, not a formal
held-out comparison or eight algorithm successes. One frozen local batch, two
physically locked single-GPU lanes, four distinct cases each; failures isolated.

Configuration and per-object source criteria:
`configs/system/native_object_state_screen.json`.
