# Chest: independent verification of frozen causal admission and key positions

The completed Dense screen established a usable open-chest/blue-cloth source
on both new seeds20260925/26. The source-review SHA is locked in
native_object_state_memory.json. Envelope is excluded for source validity,
not because of any memory-method output. Chest's visible-cut controls also
lose the open state; do not frame the follow-up as pure long-absence forgetting.

Freeze four new full509 videos: same causal descriptor admission, original
versus recent-virtual source K positions, both seeds. Reuse the two Dense
revisit videos. No manual source/target index is passed to the selector; keep
cosine floor.8,margin.05,minGap32,8GiB archive and8-frame snapshots unchanged.
Do not retune on these outputs. The new original-position branch simply uses
the previously qualified native raw installation; it must pass a real short
gate against the existing privileged original-shot output before this batch.
The new recent-virtual subclass also needs a real short gate against the old
causal controller's saved bead output: actual full latents/decoded RGB,
archive/decision sequence and payload bytes must agree. Both gates run before
either full-video lane; a failed gate is retained and blocks the four new cases.

Audit actual pre96 latent / pre381 decoded RGB against Dense, archive and
selection records, all raw H2D/D2H bytes and CPU budget. Compare source choice
and prefix between the two position variants before attributing changes to
position. Source40–47 at return96 is an offline expectation only: if the
selector chooses another version or abstains, retain that as admission behavior,
do not force the desired source or mark it a runtime bug.

Review open-lid state, blue contents, chest identity/geometry, scene contamination
and later unwanted action separately. Same source/bytes does not guarantee good
readout. This is an independent development check, not formal SOTA ranking or
proof that position remapping alone is novel. No new three-role router is added.
