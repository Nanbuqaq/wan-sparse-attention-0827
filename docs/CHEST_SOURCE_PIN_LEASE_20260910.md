# Two-case source-pin lifetime control, after the hybrid factorial

Keep exactly the two hybrid cases' conditions, selector, source admission,
recent K positions and reference constructor. At the first returned clean
commit, preserve the actually selected source pin instead of the newly generated
return chunk. Source indices/target time come from the causal installation record,
not manual arguments to the selector. No K/V is copied or rotated by the lease.

The change is after latent104 has completed. Actual first104 latents and first413
decoded RGB must match the previous hybrid. Native pin events are observed AFTER
the override, so the saved metadata reflects the effective policy, not merely
what the original pin function requested. Shapes/capacity and raw KV H2D remain
unchanged; metadata GPU writes/readbacks and sampled-witness transfers are counted.

Sample witnesses read three representative layers, all heads and four fixed
positions per source frame from the real resident KV at the intervention and
return104/112/120. They are not full-KV provenance, semantic scores or selector
inputs. Diagnostic index H2D and KV D2H must remain explicit in every report.

This changes WHICH context survives: at104 it also removes away context sooner;
later it keeps original source rather than the first generated return anchor.
It is not pure layout or age-matched representation comparison. No new key
retiming, text, seed or budget search. If late closure persists, preserve negative.
Future native cuts without a matching new installation are not overridden;
uncut intent changes and general state validation remain unqualified.
