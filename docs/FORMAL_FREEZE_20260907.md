# Formal configuration freeze, 2026-09-07

The research program remains incomplete; continue through formal 477/957,
canonical quality review, hardware-boundary outcomes and final evidence audit.
No formal sparse holdout frames existed at this freeze.

## Decisions made before formal results

- Freeze three online configurations: RAG Dense with generic optimized transfer;
  repaired legacy Final; and identical Final admission with archive-run packing
  and per-chunk union cache. CPU threads=2, pageable archive and prototype
  readiness fixes are common. Neither the archive nor stream lifetime is bounded.
- Keep grouped FA2. Batched FA2 validation at `80ecdbe` passes all eight cases
  with exact noise, latent values, ordered routes and preencode pixels. Complete
  backend service falls 42–55%, but state Final complete time regresses 3.44%.
  Mixed complete-time evidence does not pass the registered promotion gate.
- Hierarchical raw cache reduces Final KV traffic 33–42% but has mixed wall time;
  Dense's undersized global slab thrashes. Do not promote it from per-layer LRU
  estimates, zero warm H2D or component benchmarks.
- Bootstrap admission passes only 2/5 long calibration fidelity groups. Cost
  model MAPE=37.4% is over the 15% gate. Causal role prediction and automatic
  motion initialization fail their two-category gates. No fourth online slot.
- Actual offline oracle Tether videos and quarter review are complete. Both
  addressing variants keep the full candidate KV and cannot enter online Pareto.
- Production MP4 discrepancies are downstream of identical raw VAE pixels.
  Exact codec cause is not proven. Formal fidelity uses canonical raw pixels
  decoded from saved latents; MP4 hashes remain artifact integrity only.

## Completed hardware evidence and remaining scope

- H-pool dataflow matrix: 72/72, missing=0, actual runtime name **H800**. Never
  relabel it H200 from its 139.831 GiB capacity or other matching specifications.
  Its `grouped_fa2` row means prepared FA2 reference, not the production wrapper.
- RTX4090 boundary matrix: 12/12 passing, missing=0. Same frozen point indices.
- First 5kpro attempt `0e84f85`: both pre-matrix gates abort in Triton 3.2's
  unsupported-compute-capability assertion. This is an environment failure,
  not numerical or algorithm evidence. Preserve it while testing a private fix.
- Optimized Dense/Final profiles at 39/120/240 latent are complete. All six
  final-chunk first-call Nsight traces have been exported and audited. They are
  instrumented windows, not full production video time or all-denoising averages.
  Attention kernel active fractions are 4.27–4.79% Dense and 3.19–3.42% Final;
  complete wrapper ranges are 13.48–15.50% and 11.91–12.67%, respectively.

## Next execution

1. Two local GPUs run the exact three frozen modes on isolated motion/state39;
   verify state collection and identical Final routes/latents before submission.
2. Push tested immutable source. One 8-GPU formal477 batch, 24 videos, with each
   prompt/seed triplet on the same loaded model/GPU. Reverse Final system order
   in alternate lanes. Record model load separately and use private lane caches.
3. Recover and audit all cases; canonical raw fidelity plus four-quarter,
   subject/background/state and late-quarter visual review.
4. One 4-GPU formal957 batch, 12 videos, independently frozen seed; same audit.
5. Resolve hardware-specific gaps without changing shared environments; retain
   failures and evidence boundaries. Finish figures, attribution and handoff.

Facts live under the outer `results/`; the method manifest records audit SHAs.
Neither cache-only evidence nor lossless compiler changes establish new
admission, better absolute quality, or a complete co-design algorithm claim.
