# Finish a bounded condition x history factorial with only two new videos

Reuse the six completed cells: original-prompt Dense, original-prompt causal
recent memory, and past-text/no-history controls, on seeds25/26. Add only the
missing past-text + causal-recent-history cell for each seed. This is a diagnostic
of interaction, not a new independent holdout or autonomous state tracker.

Keep exactly the past-text strings, current-only descriptor selector, floor.8 /
margin.05 / minGap32,8GiB archive,8-frame snapshots and recent binding. No forced
source indices; audit whether the same source is actually chosen. Use the same
reference constructor as existing cells; do not borrow startup savings as a
hybrid algorithm speed claim. Previous negatives and all input/byte costs remain.

Compare against the SAME-current-text control: source-specific geometry/cloth
detail, scene contamination and later unwanted action, not merely blue color or
early open state. Improvement must survive both seeds without degrading late
behavior. Otherwise retain negative/mixed rather than tune wording/seeds/delta.

Exact full CLI preflight first, then one full runtime/prefix gate before the
second case. Real output reviews remain descriptive, with no SOTA/Pareto claim.
